Yi Qiao Wang
260682080

When using the run script, the output files are in the same directory as the
input file.
 
You can ignore the error message from the compiler about the executable
not being in $PATH.
